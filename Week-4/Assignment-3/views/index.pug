extends layout.pug 

block content 
    //- p(style="display:none") #{system_Notification}
    p #{system_Notification}
    form(action='/db', method='POST')
        p Email:
        input(type="text", name="userEmail") 
        p Password:
        input(type="password", name="userPw") 
        button(type='submit') submit
        